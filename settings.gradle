pluginManagement {
  repositories {
    maven { url ossimMavenProxy }
  }
}

include 'omar-disk-cleanup-app'

project(':omar-disk-cleanup-app').projectDir = "${rootProject.projectDir}/apps/omar-disk-cleanup-app" as File
